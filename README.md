# Gemini Chatbot

A simple chatbot built using Gemini API.

## Features
- Conversational chatbot
- Uses Gemini model

## Setup
1. Install dependencies
pip install -r requirements.txt

2. Add API key in .env

3. Run the project
python main.py